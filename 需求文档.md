# 表单填写器 —— 需求文档

> ⚠️ **本文档已废弃**：这是最初基于 Python CLI（Selenium/Playwright + YAML）的需求，最终产品已改为 Electron 桌面客户端。现行使用说明见 [README.md](README.md)，设计方案见 [客户端方案.md](客户端方案.md)。本文档仅作历史留存，其中 §6「硬约束」仍然适用。

> 版本：v1.0（草稿）
> 日期：2026-09-18
> 状态：已废弃（被 Electron 客户端取代）

---

## 1. 项目概述

一个基于 Python 的表单填写器。启动脚本后自动访问指定网站，根据配置文件中的「题目 → 答案」映射，自动定位并填写页面上的题目。答案与元素定位策略均放在配置文件中，脚本本体不改。

**核心诉求：**

1. 自动前往指定网站并填写题目（答案在配置文件中）
2. HTML 结构可能变化 —— 支持通过「定位策略回退链」快速适配，改配置不改代码
3. 网站可能重定向到登录界面 —— 支持更新链接
4. **只填内容，绝不自动提交** —— 最终提交由用户人工完成

**协作方式：** 用户把表单页 HTML 发来，由开发方据此微调 `selectors.yaml` 定位策略。

---

## 2. 技术选型

| 项 | 结论 | 理由 |
|----|------|------|
| 语言 | **Python** | 生态成熟、易读易改，适合「发 HTML 微调」的迭代方式 |
| 浏览器自动化 | **Playwright** | 自动等待、选择器强、支持 headless、登录态持久化、失败可截图 |
| 配置格式 | **YAML** | 比 JSON 更易读、支持注释，适合人工维护选择器与答案 |
| 模糊匹配 | 标准库 `difflib` | 无需额外依赖，满足文本模糊匹配需求 |

> 说明：现有 `form_filler.py` 为 Selenium 版，本版本迁移到 Playwright。迁移方案见 §10。

---

## 3. 目录结构

```
form_filler/
├── 需求文档.md            # 本文档
├── fill.py                # 入口脚本（含 CLI 参数）
├── requirements.txt       # 依赖清单
├── README.md              # 使用说明
├── build.bat              # PyInstaller 打包脚本（Windows）
├── .gitignore             # 忽略 profile/、output/ 等敏感/生成物
├── config/                # 配置目录（默认）
│   ├── site.yaml          # 站点/浏览器/填表行为配置
│   ├── answers.yaml       # 答案库
│   ├── selectors.yaml     # 字段定位策略（回退链）
│   └── profile/           # 登录态持久化目录（自动生成，含敏感 cookie，勿提交）
├── core/                  # 核心模块
│   ├── __init__.py
│   ├── config.py          # 配置加载/校验/目录管理
│   ├── browser.py         # 浏览器启动/访问/登录环节阻塞换链接
│   ├── locator.py         # 元素定位 + 多策略回退 + 文本模糊匹配
│   ├── filler.py          # 按题型填答案
│   ├── report.py          # 填表报告 + 未填项清单 + 截图
│   └── state.py           # 断点续填状态
├── samples/               # 本地样例表单 HTML（file:// 可跑，用于自测/验收）
└── output/                # 运行产物（自动生成）
    ├── report_*.json      # 填表报告
    ├── state.json         # 断点续填状态（结构见 §8）
    └── *.png              # 失败截图
```

---

## 4. 配置文件规范

### 4.1 `config/site.yaml`

```yaml
# 目标链接（登录环节给的新链接会自动回写到这里）
link: "https://example.com/form"

# 是否有登录环节（由用户配置，脚本不自动检测）
#   true  -> 访问原网站后阻塞，等待用户粘贴登录后的新链接
#   false -> 直接填表
has_login: false

# ---- 浏览器 ----
headless: false           # 调试 false，正式可 true
user_data_dir: "profile"  # 登录态持久化目录（相对本配置文件目录，每套配置独立，含敏感 cookie 勿提交）
slow_mo: 0                # 每步操作间隔毫秒，方便观察
wait_after_load: 1000     # 页面加载后额外等待毫秒

# ---- 反自动化检测 ----
stealth:
  enabled: true
  disable_automation_controlled: true
  maximize: true

# ---- 填表行为 ----
# 本工具「绝不自动提交」，不提供 auto_submit 开关（避免留下可改 true 的后门），
# 行为在代码中硬编码，见 §6 硬约束。
wait_timeout: 10000       # 单字段定位等待上限(ms)
screenshot_on_fail: true  # 定位失败自动截图
```

### 4.2 `config/answers.yaml`

```yaml
# 答案库。每条字段：
#   id              与 selectors.yaml 的 key 对应
#   question        题目文本（用于 text 定位策略 / 日志展示）
#   answer          答案，语义随 type 而定（见 §4.2「answer 语义」列）
#   type            题型（默认 text）
#   match_threshold 模糊匹配阈值 0~1，默认 0.6（短文本建议调高）
answers:
  - id: q1
    question: "你的姓名？"
    answer: "张三"
    type: text

  - id: q2
    question: "性别？"
    answer: "男"
    type: radio

  - id: q3
    question: "满意度？"
    answer: ["非常满意", "满意"]   # select 的列表 -> 候选池，每次随机选一个
    type: select
```

**支持的题型（type）：**

| type | 说明 | answer 语义 |
|------|------|-------------|
| `text` | 单行文本 / 数字输入 | 单值 = 固定文本 |
| `textarea` | 多行文本（简答） | 单值 = 固定文本 |
| `radio` | 单选题（单选按钮） | 单值 = 选中该项 |
| `judge` | 判断题（对/错，等同 radio） | 单值 = 选项文字（「对/错」「是/否」等，需与页面一致） |
| `checkbox` | 多选题 | 列表 = **全部勾选**；单值 = 勾选一项 |
| `select` | 下拉框 | 单值 = 选中该项；列表 = 候选池（每次随机选一个） |
| `date` | 日期输入 | 单值 = 日期文本（默认 `fill()`，日历控件待 HTML 后定） |
| `file` | 文件上传 | 单值 = 本地文件路径 |
| `matrix` | 矩阵题（问卷常见） | 待真实 HTML 后细化 |

> **说明：**
> - 「列表」语义随题型不同：`checkbox` = 全部勾选，`select` = 随机选一，二者不混用。
> - `match_threshold` 对短文本（≤4 字）不敏感，默认 0.6 易误匹配，建议短题设 0.8 以上，或 `text` 策略优先精确包含匹配、模糊匹配兜底。
> - `date` / 搜索型下拉 / `file` / `matrix` 的填写方式依赖真实 HTML，实现前需确认。

### 4.3 `config/selectors.yaml`

```yaml
# 每个 key 对应 answers.yaml 的 id，值为定位策略列表（回退链），逐个尝试直到命中。
# 策略类型：
#   css:   CSS 选择器
#   xpath: XPath
#   text:  按问题文本模糊匹配定位
# 改版后只需追加新策略，旧的不删。
selectors:
  q1:
    - css: "#q1 input"
    - text: "姓名"

  q2:
    - css: "input[name='gender']"
    - text: "性别"
```

---

## 5. 运行流程

```
① 读取 site.yaml 的 link → 浏览器访问原网站
② 若 has_login = true：
      阻塞，input("登录完成后请粘贴新链接（回车则沿用原链接）：")
      · 粘贴新链接 → 回写 site.yaml 的 link（持久化）→ 访问新链接
      · 直接回车   → 沿用原链接
③ 遍历 answers.yaml 逐题定位 + 填写
④ 输出填表报告（成功 / 未找到 / 失败 + 截图）
⑤ 结束：浏览器保持打开，由用户人工核对并自行点击提交
```

---

## 6. 核心设计原则（硬约束）

1. **不检测登录/重定向** —— 是否换链接完全由 `has_login` 配置决定，脚本不做任何 URL 判断。
2. **绝不自动提交** —— 只填内容，不触碰 submit 按钮、不触发 `form.submit()`、不模拟回车提交；代码中不提供任何提交开关。
3. **配置与代码分离** —— 网址、答案、选择器全部在配置文件，脚本本体不改。
4. **链接持久化** —— 用户给的新链接回写 `site.yaml`，下次运行直接沿用。
5. **选择器回退链** —— 单个字段可配多条定位策略，HTML 改版只加不删。
6. **显式等待** —— 用 Playwright 等待机制 + 可配置的 `wait_after_load`，不用固定 `time.sleep`。
7. **合规边界** —— 仅用于用户本人授权或本人填写的表单；`stealth` 反检测仅作兼容性用途，不用于批量刷量、投票等违规场景。

---

## 7. 功能需求清单

| # | 功能 | 说明 | 状态 |
|---|------|------|------|
| 1 | 配置驱动填表 | 答案/选择器/网址均在配置文件 | 待实现 |
| 2 | 登录环节阻塞换链接 | `has_login=true` 时访问后阻塞等待新链接，回写配置 | 待实现 |
| 3 | 不自动提交 | 填完保持浏览器打开，用户人工提交 | 待实现 |
| 4 | 干跑模式（dry-run） | `--dry-run` 只验证选择器命中，不真正填写 | 待实现 |
| 5 | 选择器自检报告 | 逐题输出命中/未命中 + 命中的策略 | 待实现 |
| 6 | 未填项清单 | 报告列出所有未完成字段 | 待实现 |
| 7 | 断点续填 | `--only-missing` 只填上次未完成的字段 | 待实现 |
| 8 | 登录态持久化 | `user_data_dir` 保存 cookie，减少重复登录/换链接 | 待实现 |
| 9 | 答案模糊匹配 | 题目/选项文字微小变化也能命中（difflib） | 待实现 |
| 10 | 反自动化检测 | stealth 参数（disable-blink-features 等） | 待实现 |
| 11 | 多套配置 | `--config 名字` 切换 `config/<名字>/` | 待实现 |
| 12 | 打包 exe | PyInstaller 打包（build.bat） | 待实现 |
| 13 | 题型扩展 | 覆盖 text/textarea/radio/judge/checkbox/select/date/file/matrix | 待实现 |
| 14 | 失败截图 | 定位失败自动截图到 output/ | 待实现 |

---

## 8. 报告与状态文件规范

### 8.1 填表报告 `output/report_*.json`

每次运行生成一份（文件名含时间戳），记录每题定位与填写结果，供 `--only-missing` 与人工核对使用：

```json
{
  "run_time": "2026-09-18T10:00:00",
  "site_link": "https://example.com/form",
  "dry_run": false,
  "fields": [
    {
      "id": "q1",
      "status": "filled",
      "strategy": "css",
      "answer": "张三",
      "error": null
    }
  ],
  "summary": { "total": 3, "filled": 2, "missing": 0, "failed": 1 }
}
```

`status` 三态：

- `filled` —— 成功定位并填写
- `missing` —— 所有回退策略均未命中（或在 `--only-missing` 下被跳过）
- `failed` —— 已定位但填写报错（如 `file` 路径无效）

### 8.2 断点续填状态 `output/state.json`

```json
{ "q3": "failed", "q5": "missing" }
```

- 只记录**未完成**字段（`missing` / `failed`），`filled` 不落盘。
- `--only-missing` 读取此文件，只填其中列出的字段；填写成功后从文件移除。
- `--reset` 删除此文件并退出；`--dry-run` 不写 state。

---

## 9. CLI 接口

```
python fill.py                      # 正常填表（按配置）
python fill.py --dry-run            # 干跑：只验证选择器命中，不填写
python fill.py --only-missing       # 只填上次未完成的字段（断点续填）
python fill.py --link URL           # 临时覆盖 site.yaml 的 link
python fill.py --config 名字         # 使用 config/<名字>/ 下的配置
python fill.py --reset              # 清空续填状态后退出
```

---

## 10. 现有代码迁移

目录中已有一个可运行的 Selenium 版 `form_filler.py`（含 `config.json`、`answers.json`）。迁移要点：

| 旧版 | 新版 | 变化 |
|------|------|------|
| Selenium + ChromeDriver | Playwright | 免手动装 driver |
| `config.json` | `config/site.yaml` | JSON → YAML |
| `answers.json`（答案池随机选） | `config/answers.yaml` | 支持固定答案 + 随机池 |
| 按 `input_{字段名}`/`name` 定位 | `selectors.yaml` 回退链 | 定位更灵活抗改版 |
| **自动提交** `submit_form()` | **不提交** | 硬约束变化 |
| 每次运行询问是否换链接 | `has_login` 配置驱动阻塞换链接 | 逻辑更明确 |

> **待确认：** 旧脚本 `form_filler.py` 是「替换为新版」还是「保留共存」（见 §12）。

---

## 11. 依赖清单

```
playwright>=1.40
PyYAML>=6.0
```

> 首次运行需执行 `playwright install chromium`（下载浏览器内核）。

> **打包提示：** PyInstaller 打包 Playwright 需额外处理浏览器内核路径、`asyncio` 事件循环、驱动与内核版本匹配等问题，`build.bat` 需单独调试，作为可选交付项（见 §13 验收标准）。

---

## 12. 待确认 / 待补充

1. **旧脚本处理方式**：替换为新版 / 保留共存 / 备份后删除（未定）。
2. **表单页 HTML**：需用户提供，据此补全 `selectors.yaml` 与真实填表逻辑；并同步提供一份精简样例 HTML 放入 `samples/`。
3. **登录方式细节**：本设计统一走 `has_login` 半自动（人工登录后粘贴新链接），不自动登录。
4. **题型最终范围**：当前按 §4.2 九类实现，矩阵题、搜索型下拉等复杂题型需真实 HTML 后细化。
5. **合规边界**：本工具仅用于填写用户本人授权或本人填写的表单，不得用于批量刷量、投票等违反目标平台规则的行为（见 §6）。

---

## 13. 验收标准

- [ ] 启动后能访问配置中的链接
- [ ] `has_login=true` 时能阻塞等待新链接并持久化
- [ ] 逐题按 `answers.yaml` + `selectors.yaml` 填写，题型覆盖 §4.2 九类，且每类至少一条 `samples/` 样例 HTML 能填对
- [ ] 全程不触发提交，浏览器填完后保持打开（代码中不存在任何 submit 触发点）
- [ ] `--dry-run` 能输出选择器命中/未命中报告，且不写 state
- [ ] 定位失败有截图 + 未填项清单，报告结构符合 §8
- [ ] 断点续填：中断后 `--only-missing` 只补填 state 中未完成字段，填写成功即从 state 移除
- [ ] 多套配置（`--config`）彼此隔离，profile 目录互不串用
- [ ] `.gitignore` 覆盖 `profile/`、`output/` 等敏感/生成物
- [ ] 打包 exe 可运行（PyInstaller 兼容性受阻时作为可选交付项，不影响其余验收）
