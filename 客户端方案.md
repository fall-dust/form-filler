# 表单填写器 —— Windows 客户端方案

> 版本：v1.2
> 日期：2026-09-19
> 状态：已基本成型
> 关联文档：[README.md](README.md)（使用说明）、[需求文档.md](需求文档.md)（原始需求，已废弃）、[任务配置方案.md](任务配置方案.md)（任务存储改版 M7，已实施；本文 §9 已按它更新）、[表单覆盖增强方案.md](表单覆盖增强方案.md)（M8 覆盖增强 + M8.6 自动提交可选，均已实施）

---

## 1. 总体定位

一个 Electron 桌面客户端，把「填写网页表单/问卷」收敛成一句话：

**拿到表单 HTML（抓取或粘贴）→ 生成提示词 → AI 产出题目/答案配置 → 一键填写**

核心判断：**HTML 结构千变万化，靠规则去解析不可靠**（实测多选题的 A/B/C/D 常被拆错、题干识别不出）。所以把「理解 HTML」这一步交给 LLM——客户端只负责三件事：生成精准提示词、导入 AI 结果、执行填写。

原「cheerio 启发式解析」方案已废弃删除。

## 2. 技术栈

| 项 | 结论 | 理由 |
|----|------|------|
| 壳 | Electron | 跨端 UI 成熟 |
| 前端 | React + TypeScript + Vite（electron-vite） | 组件化、类型安全 |
| 自动化 | Playwright（Node 版） | Node 一等公民，locator 强 |
| 配置生成 | 提示词 + 任意 LLM | 理解任意 HTML 结构 |
| 配置存储 | JSON（`userData/tasks/<任务id>/`） | Node 原生；任务即文档，自动落盘 |
| 打包 | electron-builder（NSIS + portable） | 成熟 |

## 3. 架构与进程模型

```
┌──────────────────────── Electron App ──────────────────────────┐
│                                                                │
│  ┌───────────────┐   IPC (contextBridge)   ┌────────────────┐  │
│  │  Renderer      │ ◄──────────────────────► │  Main (Node)    │  │
│  │  React UI      │   fill:run / fill:stop   │                 │  │
│  │  · 步骤流水线  │   fill:need-login        │  ┌───────────┐  │  │
│  │  · 题目/答案   │   tasks:* / trash:*      │  │ fill-engine│──┼──► Playwright
│  │    导入表格    │   clipboard / screenshot │  │ locator    │  │    Chromium
│  │  · 任务标签    │   fill:progress ─────────┼─►│ fillers    │  │    （独立窗口）
│  └───────────────┘   tasks:patch / stop     │  └───────────┘  │  │
│                                              │  ┌───────────┐  │  │
│                                              │  │ tasks 存储 │  │  │
│                                              │  │(自动保存/ │  │  │
│                                              │  │ 快照/回收站)│ │  │
│                                              │  └───────────┘  │  │
└────────────────────────────────────────────────────────────────┘
```

**关键点：**

1. **双进程职责分离**：Playwright、文件读写、**任务持久化**都在 main 进程；renderer 只做展示、提示词生成与交互。安全基线 `contextIsolation: true` + `nodeIntegration: false` + `sandbox`。
2. **异步模型**：填充进度由 main 通过 `webContents.send('fill:progress')` 推给 renderer，UI 不卡。
3. **提示词在 renderer 本地生成**（纯字符串拼接），不经过 main、不联网；生成后由你复制给任意 AI，返回的 JSON 再贴回导入并由本工具校验。
4. **多任务并发**：renderer 每个任务标签独立状态；main 用 `Map<runId, …>` 隔离每次运行的取消标志，进度事件按 `runId` 路由，多任务可同时各开一个浏览器并行填写 —— 每个任务有**独立的浏览器 profile 目录**（`<userData>/profile/<taskId>`），互不干扰，登录态也各归各的。
5. **自动持久化**：renderer 改动立即发 `tasks:patch`，main 侧防抖 500ms 后**同步原子写**（临时文件 + rename），退出前 `before-quit` flush，不会丢最后一段输入；无「保存 / 加载」按钮。

## 4. 核心流程

```
① 拿到表单 HTML（启动浏览器打开目标链接后一键抓取，或手工粘贴）→ 生成「元素/题型」提示词 → 问 AI → 贴回题目 JSON → 导入
② （可选）填作答目标 → 生成「答案」提示词 → 问 AI → 贴回答案 JSON → 导入
③ 运行 → Playwright 逐题定位 + 填写 → 报告/日志/失败截图
```

> 顶部标签栏就是任务列表，每个任务独立走上面三步，可并发运行。改动自动落盘（无保存按钮）；「×」删除进「历史任务」可撤销；侧栏「清空内容」重置当前任务但保留任务本身与历史版本。重命名统一走「双击标签栏」；侧栏不放任务名输入框，也不重复放「启动浏览器 / 抓取当前页 HTML」（那两个在右侧步骤 1）。
>
> ① 的 HTML 不必手工复制，分两步：**启动浏览器**（只打开、不抓取 —— 因此**没有题目/答案也能先把页面开起来**去登录、翻页、看清结构）→ 页面显示出来后**抓取当前页 HTML**。抓取**只在用户点击时发生**，且会先等页面真正加载好（未就绪则如实报错，不抓空壳）；覆盖前自动把上一份 HTML 与当时的题目存进历史版本（`pre-grab`，回滚可连页面一起还原）。抓取成功后还会用页面标题给任务起个名（仅当名字仍是自动生成的时候）。
>
> **多页问卷**：抓取会按内容自动落到对应的**页存档**（见过的页只更新 HTML、题目继续沿用；没见过的存成新页），所以「翻到第 3 页再抓」不会覆盖第 1、2 页；识别到的「下一页」类按钮会显示在步骤 1，点它即在浏览器里翻页（也可「翻页并抓取为新页」一次做完）。每一页各自留着自己的题目与答案，切回旧页直接沿用 —— 不必重新生成。

**登录**不设开关、不打断填写：每个任务固定用一个浏览器 profile 目录（`<userData>/profile/<taskId>`），**登录一次长期有效** —— 关掉窗口、点「结束会话」、改过目标链接，下次打开都还带着 cookie。换账号或登录态失效时，左栏点「清除登录态」（关窗口 + 删该目录）。（早先那套「勾了就一直暂停、等你粘贴登录后的新链接」已移除：它与「先启动浏览器再抓取」这条主路重复，且暂停期间「停止」无法中断。）

两段提示词对应两个 AI 产物：

- **题目配置**（`ParsedQuestion[]`）：`id / question / type / options / optionValues / selectors`
- **答案**（`{ qid: 答案 }`）：单选/下拉给选项文字，多选给数组，文本给字符串

## 5. 配置格式（AI 输出）

题目对象结构（与填写引擎一致）：

```json
{
  "id": "q1",
  "question": "题干",
  "type": "radio",
  "options": ["A、…", "B、…"],
  "optionValues": ["A", "B"],
  "selectors": [{ "css": "input[name='q1']" }]
}
```

- `selectors` 是定位回退链，优先 `id / name / data-*`，其次 `xpath / text`。
- 选项由 AI 给出（界面下拉用）；若某题缺选项，填写引擎会**运行时从页面动态读取**兜底。

## 6. 填写引擎（fill-engine）

| 模块 | 职责 |
|------|------|
| `engine` | 编排：启动（或复用会话）/导航/逐题填写/报告汇总/自动提交 |
| `browser` | 启动浏览器与打开链接（`launchBrowser` / `openPageAt`）—— 填写路径与「启动浏览器」共用，保证两处启动行为一致 |
| `locator` | 回退链定位（css/xpath/text 模糊匹配），支持 iframe 透明定位；**选择题「整组优先」**（某条只命中单个选项时继续探测，取命中最多者）；属性读取 2s 上限 |
| `fillers` | 14 类题型填写 + 运行时动态读选项 + 多级点选回退（含同容器兄弟 label，兼容 TDesign/腾讯问卷）+ 自定义下拉浮层（el-select/antd/TDesign `.t-select__dropdown`，带搜索框先过滤）（详见 [表单覆盖增强方案.md](表单覆盖增强方案.md)） |
| `verify` | 填写后回读校验（严格题型不符则重填一次） |
| `submit` | 提交按钮定位与点击（`autoSubmit`，默认关闭）+ 点击后效果验证 |
| `session` | 浏览器会话注册表：按任务记住窗口，多页问卷「接着填」时复用（不重开、不重新导航）；`ensureSession` 在无存活会话/链接改动时按目标链接打开 —— 它**只管「打开」，不管抓取**（所以题目/答案还没生成也能先把浏览器开起来） |
| `grab` | 抓取当前页 HTML（主 frame + 含表单控件的 iframe，剥 script/style 等噪音）；抓前先 `waitForGrabReady` 等页面真正长好（`readyState` 不再 loading 且有表单控件/有正文），未就绪则如实报错而不是抓回空壳；纯读取，不点按钮不导航。抓完顺手识别一次翻页按钮（失败不影响抓取） |
| `next-button` | 识别「下一页」类按钮（**只读**：关键词打分 + 命中「提交/完成/返回/上一步/取消」一律排除 + 隐藏/0 面积排除 + 禁用照实标注；主 frame 优先，再去子 frame）与点击（标记选择器 → `role=button` 文本 → 文本派发三级回退）。**只在用户点界面 chip 时点击**，程序不自己翻页 |
| `similarity` | difflib 式文本相似度（LCS 比例） |
| `state` | 断点续填状态 |
| `report` | 报告 JSON + 失败截图 |

**多级点选回退**（兼容 Element UI 等隐藏原生 input、被固定 header 遮挡的场景）：普通 `check()` → `check({ force: true })` → 点外层 `label` → 点 `label[for]` → JS 派发 `click`。

## 7. 浏览器

| 方案 | 说明 |
|------|------|
| 随包 Chromium（默认） | Playwright 自带内核，`extraResources` 打进 `resources/browsers/`，首启免下载 |
| 系统 Edge/Chrome（瘦身） | 配置 `channel: msedge/chrome` 复用系统浏览器，省约 200MB |

> 打包要点：`playwright`/`playwright-core` 必须用 `asarUnpack` 解出 asar——Playwright 的 driver 子进程在 `ELECTRON_RUN_AS_NODE` 模式下读不了 asar，否则打包后「开始填写」会无响应。

### 7.1 体积控制（M9）

安装包体积几乎全由「Electron 运行时 + 随包 Chromium」决定，因此按「只删本项目链路绝对用不到的文件」原则做无损裁剪：

| 措施 | 落点 | 收益 |
|------|------|------|
| `electronLanguages: [zh-CN, en-US]` | electron-builder 内置 | 解包 −80 MB（60+ 语言包） |
| 随包 Chromium 只留 zh-CN/en-US 语言包 | `scripts/lib/prune.mjs`（afterPack + 复制时各跑一次） | −40.8 MB |
| 删安装器与系统集成辅助 exe（setup/chrome_proxy/pwa_launcher/notification_helper/elevation*/chrome_wer） | 同上 | −22 MB |
| 运行时依赖由 `playwright` 换为 `playwright-core`，并删打包副本里的 `lib/vite`（报告/Trace 查看器）与 `types/*.d.ts` | package.json + afterPack | −20 MB |
| `compression: maximum` + 关闭差分包 | electron-builder.yml | 安装包进一步缩小，构建更慢 |

保留不动的是任何可能被渲染/GPU 路径加载的 DLL（`dxcompiler.dll`、`vk_swiftshader.dll`、`d3dcompiler_47.dll`、`libEGL/libGLESv2`）与 `hyphen-data`；要极限瘦身可设 `PRUNE_TIER=aggressive`（再省约 34 MB，WebGPU/软件渲染兜底有极小概率受影响）。

裁剪必须可回归：`npm run check:browser` 会用**裁剪后的** `chrome.exe` 起来跑一遍「打开页面 → 填写 → 读回 → 截图」，确认中文渲染与自动化都正常。

## 8. 硬约束

1. **自动提交默认关闭** —— 默认只填内容，填完浏览器保持打开，由用户人工核对并手动提交（原「绝不自动提交」硬约束）；应用户要求改为**可选**（M8.6）：`autoSubmit` 开启且全部题目填写成功时才自动点击提交按钮（三级匹配定位提交按钮），存在未完成项一律跳过并记录原因。代码中开关默认 false，不做任何自动重试。
2. **配置与代码分离** —— 网址、题目、答案、选择器全部走配置（AI 产出 / 界面导入），脚本本体不改。
3. **合规边界** —— 仅用于用户本人授权或本人填写的表单；`stealth` 仅作兼容性用途。

## 9. 数据与状态

存到 `app.getPath('userData')`：

```
userData/
├── tasks/
│   ├── index.json                    # 任务索引（名称/题数/更新时间，列表页只读它）
│   ├── settings.json                 # 回收站保留期等设置
│   └── <任务id>/
│       ├── config.json               # 链接 + 开关 + 页面存档（pages/activePageId）+ 当前页题目/答案/指纹（原子写）
│       ├── pages/<pageId>.html       # 多页问卷的页面存档：一页一份 HTML（旧 source.html 迁到这里）
│       ├── source.html               # 旧布局遗留（只读透；写入时自动迁到 pages/p1.html）
│       └── snapshots/                # 历史版本：<ts>.json 配置快照（默认最多 20 份）+ <ts>.html「抓取前」存的页面源码
├── trash/<任务id>/                   # 软删除（整目录 move 进回收站，可恢复/撤销）
├── migration.json                    # 旧 projects/ 的迁移记录
└── output/                           # report_*.json + 失败截图 + state.json + error.log
```

**任务 = 标签 = 落盘实体**：没有「项目」这个第二容器，也就没有保存 / 加载 / 删除按钮。删除是软删除（进 `trash/`，默认保留 30 天，可撤销）；导入题目/答案等关键动作会自动留快照，可回滚；导出 / 导入单文件用于换机与交接。完整设计见 [任务配置方案.md](任务配置方案.md)。

## 10. 里程碑（全部完成）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 骨架 | Electron + React + IPC + 安全基线 | ✅ |
| M1 智能解析 | ~~cheerio 解析器~~ → 已废弃，改「提示词 + LLM」 | ⚠️ 替换 |
| M2 填写引擎 | 回退链定位 + 题型填写 + 进度 + 截图 + 断点续填 | ✅ |
| M3 UI 整合 | 提示词生成器 + 题目/答案导入 + 运行页（登录弹窗已由「登录态持久化」取代，见文末补记） | ✅ |
| M4 打包 | NSIS 安装包 + portable exe + 随包 chromium | ✅ |
| M5 | ~~内置 AI（DeepSeek 直连）~~ 已移除；题目/答案改由手动提示词通道导入 | ⚠️ 已移除 |
| M6 界面重构 | 左配置栏 + 右步骤流水线单页布局；去掉运行二级 tab，填写状态行内实时亮灯 | ✅ |
| M7 任务存储 | 任务即文档自动落盘 + 回收站软删除与撤销 + 自动快照与回滚 + 导入导出 + 删除「项目」概念 | ✅ |
| M8 覆盖增强 | iframe 透明定位 + 14 类题型（新增 slider/rate/switch/richselect/richtext）+ 面板式日期回退 + 填写后回读校验重试 | ✅ |
| M8.6 自动提交可选 | 三级匹配定位提交按钮（默认关闭、仅全部题目成功才点）+ 点击后效果验证（结论落报告字段 `submitConfirmed`）+ 响应未确认时如实提示 | ✅ |
| 多页问卷「接着填」 | 按任务记住浏览器会话，再次运行复用同一窗口、在其当前页继续填（不重开、不重新导航）；**启动浏览器与抓取分离**（`fill:session:open` 只打开不抓取，题目/答案还没生成也能先把页面开起来）；抓取只在用户点击时发生，且抓前先等页面真正加载好（不抓空壳）；**抓取覆盖前自动存档**（`pre-grab` 快照连 HTML 一起存，回滚可连页面还原）；抓取成功后按页面标题自动命名任务（`nameAuto`，用户手动改过的名字不动）（未占里程碑编号） | ✅ |

| 登录态持久化 | 删掉「有登录环节」开关与登录弹窗（与「先启动浏览器再抓取」主路重复，且暂停期间「停止」无法中断）；改为**每任务一个固定浏览器 profile**（`<userData>/profile/<taskId>`）—— 登录一次长期有效，重开浏览器不用重登；左栏「清除登录态」用于换账号（关窗口 + 删该目录）。`closeSession` 改走 CDP `Browser.close`，让 Chromium 真正退出、释放 profile 目录锁（未占里程碑编号） | ✅ |
| 页面存档 + 翻页识别 | **多页问卷一页一档**：每页各自留着自己的 HTML / 题目 / 答案（`tasks/<id>/pages/<pageId>.html`），可一页一页填、翻回旧页直接沿用（不必重新生成题目）。抓取按内容自动落页（`renderer/src/pages.ts`：精确指纹 → 只更新 HTML；结构签名 → 认作同一页并继承题目；空占位页 → 填进去；否则新建页）；识别「下一页」类按钮并显示成 chip，点它才翻页（另附「翻页并抓取为新页」）；重复题的答案自动从上一页带入（未占里程碑编号） | ✅ |

> M8 详细设计见 [表单覆盖增强方案.md](表单覆盖增强方案.md)。

> 「步骤对钩回退」与「多页问卷接着填」都未占里程碑编号：前者给任务记两个内容指纹（`questionsFor` = 生成题目时的 HTML 指纹、`answersFor` = 生成答案时的题目指纹，判定纯函数见 `src/renderer/src/freshness.ts`）—— 对钩只代表「当前有效」，上游一变下游对钩自动收回并退回待办（只改状态、不删数据；旧数据载入时按「当前内容就是它的来源」回填一次，另留「其实还是同一页」的手动确认口子）。

## 11. 主要风险

1. **AI 输出不稳定**：JSON 格式偶有偏差。→ 导入侧做容错校验 + 失败自动补救重试一次，且人工可在表格里修正。
2. **包体积 ~225MB**：随包 Chromium。→ 可选 `channel` 复用系统浏览器瘦身。
3. **网站反自动化**：stealth 只能缓解。→ 「默认人工核对后提交（自动提交须显式开启且有全部成功前置条件）」作合规缓冲。
4. **模型更替**：本工具不绑定任何模型（提示词通道与模型无关），换模型不影响既有配置。
5. **登录态持久化的边界**：登录信息落在每任务的 profile 目录（`<userData>/profile/<taskId>`，单个几十 MB，含敏感 cookie，**不随「导出」外传**）。同一目录不能被两个浏览器实例同时打开，所以**别并发跑同一个任务**。更关键的是它**只对带有效期的 cookie 有效** —— 站点若用「会话 cookie」（无 `max-age`/`expires`）承载登录，Chromium 从不把它写进磁盘，重开仍需登录；这类站点只能靠不关窗口、不点「结束会话」维持。
