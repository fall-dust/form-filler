# 表单覆盖增强方案（M8）

> 版本：v1.2（**已实施**：M8.1–M8.5 全部完成，版本 0.4.0）
> 日期：2026-09-19
> 编号说明：本项原编号 M7，因「任务存储改版」先占用了 M7，故顺延为 **M8**。同期追加实施的 **M8.6（自动提交可选）**见 §5（另有已回滚的「分页问卷支持」，同见 §5）。
> 目标：把表单覆盖率从"常见问卷"提升到"尽可能多的在线表单"
> 关联文档：[客户端方案.md](客户端方案.md)、[AI内置方案.md](AI内置方案.md)

## 1. 现状诊断（基于代码逐条核实）

| # | 短板 | 代码位置 | 影响 |
|---|------|----------|------|
| G1 | **只在主 frame 定位**：`resolveQuestion(page, …)` 只搜主文档，iframe 内的表单全部「定位未命中」 | `locator.ts` | 各类嵌入式表单/问卷组件（很多 SaaS 表单都在 iframe 里）全军覆没 |
| G2 | **只支持原生下拉**：`fillSelect` 读 `<option>`，el-select / antd Select 等"div 模拟的下拉"无法处理 | `fillers.ts` | 主流组件库最高频的控件之一 |
| G3 | **缺题型**：滑条/评分（含星级）、开关 switch、级联 cascader、富文本 contenteditable 都不支持，AI 也只能强行归类成已有九类 | `types.ts`、`fillers.ts` | 问卷星评分题、NPS 滑条、设置开关等 |
| G4 | **date 靠 `fill()`**：只对原生 `input[type=date]` 有效；el-date-picker 等弹出面板式日期选择无效 | `fillers.ts` | 面板式日期选择器失败 |
| G5 | **填写前不滚动、后不校验**：元素可能在视口外被固定 header 遮挡；填完不回读验证，错了也不知道错在执行层 | `engine.ts` | 失败率虚高、误报 filled |
| G6 | **提示词只描述九类题型**：AI 见到滑条/评分/自定义下拉时无从归类，或错标 type | `prompt.ts`（renderer） | 上游识别不准，下游执行必然失败 |
| G7 | **多步表单不能自动翻页**：智能问卷答一页点"下一页"，引擎按顺序填完当前页就结束 | `engine.ts` | 只能人为分段跑 |
| G8 | 定位等待最多 3s/策略：慢加载的选项列表/懒渲染题目可能超时 | `locator.ts` | 偶发定位失败 |

> **不做的**（合规与技术边界，与需求文档 §6 一致）：验证码/人机校验破解、反风控对抗、Canvas 内渲染控件。（原列的「自动提交」已应用户要求改为**可选**，见 §5 M8.6。）

## 2. 改进设计

### M8.1 定位与执行健壮性（地基，收益最大）

- **iframe 透明定位**：`resolveQuestion` 改为 `frames = [page, ...page.frames()]` 逐 frame 尝试回退链（浅层优先），`Resolved` 增记 `frame`；填写时用该 frame 的 locator。同时规避 `about:blank` 与跨域无关 frame 的无谓扫描。
- **滚动到可见**：填写前 `loc.scrollIntoViewIfNeeded()`，再判断元素是否被遮挡（`elementFromPoint` 粗检），被遮挡时对遮挡层 `evaluate(el => el.style.pointerEvents='none')` 级处理不可行——改为**直接坐标点击回退**已有 `checkOption` 兜底，本次只做 scroll + force。
- **填写后校验 + 重试一次**：按题型回读 DOM（input.value / checked / aria-valuenow / textContent），不匹配则重填一次并记录 `verified` 字段进 `FieldResult`；两次仍不符降级为 `failed`（带截图）。校验器与 fillers 同文件放 `verify.ts`。
- **等待放宽**：定位等待参数化默认 3s→5s/策略，且对 `select/radio` 动态读选项增加 `waitForOptions`（轮询选项数 > 0，最多 5s）。

### M8.2 新题型 fillers（types.ts + fillers.ts）

新增 5 个 `QuestionType`，每个配独立 filler + verifier：

| type | 识别特征（提示词里给 AI 的判据） | 填写策略 |
|------|------|------|
| `slider` | `input[type=range]`、`.el-slider`、`.ant-slider` | 优先 `fill()`/`evaluate` 设 value + 派发 `input/change`；UI 库滑条回退为**键盘方向键**（focus 后按 Left/Right 步进）或拖动手柄坐标 |
| `rate` | 星级 `.el-rate`、`.ant-rate`、star 图标组 | 点第 N 颗星（多级点选回退复用 `checkOption`） |
| `switch` | `.el-switch`、`.ant-switch`、checkbox 样式开关 | 目标态与当前态不一致才点击（读 `aria-checked`/class） |
| `richselect` | div 模拟下拉：`.el-select`、`.ant-select`、`role="combobox"` | 点击展开 → 从浮层容器读选项文本 → 相似度匹配 → 点选 → 等浮层收起；支持多选（再次展开追加） |
| `richtext` | `contenteditable`、`.ql-editor`、`.tox-editable`、iframe 编辑器 | click + `keyboard.insertText`（兼容 React 受控），编辑器为 iframe 时切入其 frame 填写 |

- `date` 增强面板式回退：`fill()` 失败后尝试 `input.click()` → 在弹层里按"年/月/日"文本点选（限定 `.el-date-picker`、`.ant-picker` 等常见容器 + 通用可见日期单元格匹配）。
- `cascader` 列为 M8 候选（需要路径型答案 `["a","b","c"]`，改动面大，性价比当前不高）。

### M8.3 提示词升级（renderer/prompt.ts）

- 元素/题型模板补充 5 个新题型的**识别特征表**与输出格式约定（slider 数值、rate 星数、switch true/false、richselect 选项数组、richtext 纯文本）。
- 明确告知 AI：遇到 iframe 说明、无法判断的控件标注 `hint`，不要强行归类。
- 答案提示词模板按新题型给出示例值，减少二次纠正。

### M8.4 测试

- `samples/form_sample.html` 扩充：新增 slider/rate/switch/richselect(el-select 样式)/richtext/面板式日期/iframe 嵌入子表单 七个样例。
- 单测：verify.ts 各题型校验逻辑；richselect 浮层读取（本地 mock HTML）。
- e2e：现有 9 题样例保持全绿 + 新 7 题样例真实填写回读校验。

### M8.5 文档与版本

- README「支持题型」表格更新（14 类）；客户端方案.md 里程碑补 M8；版本 0.4.0。

## 3. 里程碑与验收

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M8.1 | iframe 定位 + 滚动 + 填写校验重试 + 等待放宽 | iframe 样例 e2e 通过；遮挡场景截图不再误报 filled |
| M8.2 | 5 个新题型 filler + date 面板回退 | 新样例全部真实填写成功且回读一致 |
| M8.3 | 提示词模板升级 | 新题型在提示词中可被 AI 正确归类（人工抽验） |
| M8.4 | 样例扩充 + 单测 + e2e | 全部测试绿 |
| M8.5 | 文档 + 0.4.0 | README/方案文档同步 |
| M8.6 | 自动提交可选（应用户要求追加） | 仅全部成功才点击；有未完成项跳过并说明原因 |

> 验收结果：M8.1–M8.6 全部达成。全量测试 **100 passed / 9 文件**（含 5 个 e2e），`tsc` node+web 全绿，`electron-vite build` 成功。

## 4. 风险

1. **richselect 各组件库浮层结构差异大**：按 `.el-select-dropdown / .ant-select-dropdown / [role=listbox]` 三层约定 + 通用浮层兜底；失败会明确报错并截图，不会静默填错。
2. **slider 的 UI 库步进差异**：键盘步进策略天然适配离散滑条；连续滑条用百分比坐标拖动兜底。
3. **iframe 跨域安全**：Playwright 对跨域 iframe 的 frame 对象操作不受浏览器同源策略限制（CDP 层），但个别站点 CSP 可能拦截注入——降级为报告失败并截图，与现有行为一致。
4. **改动面控制**：M8.1–M8.5 的改动限于 fill-engine 与 prompt 模板，未动业务流、AI 模块、UI 布局。（**追加的 M8.6 不在此限**：它另动 tasks 存储与左栏开关。）

## 5. 追加实施记录（同期完成，不属原 M8.1–M8.5 范围）

### M8.6 自动提交可选

原「绝不自动提交」硬约束应用户要求推翻。`FillConfig.autoSubmit`（默认 false）；新增 `fill-engine/submit.ts` 三级匹配提交按钮（原生 submit → 组件库主按钮 → 按钮文本，排除取消/重置类）；**仅当 missing+failed===0** 才点击，否则跳过并写 `submitError`；报告增 `submitted`/`submitConfirmed`/`submitError`/`submitNote`。点击后做效果验证（submit 事件 / 网络请求 / 页面跳转 / 按钮禁用），结论落在 **`submitConfirmed`**（`true`=已确认页面响应；`false`=点了但无法确认），一个都没观测到时**不谎报成功**，同时产出 `submitNote`（写明点击的按钮文字 + 请人工确认），界面显示橙色「已点击提交（结果未确认）」。字段独立成项而非只藏在文案里，便于后续接提交状态机或按结果分支（界面判定对没有该字段的旧报告保留「按 submitNote 反推」的兼容）。存储层 `TaskConfig`/`ExportedTask` 同步 `autoSubmit`（可选字段，兼容旧导出文件）。

### 分页问卷支持（已回滚）

曾实现「点『下一页』才渲染后续题」的分步问卷支持三阶段：P0 按页推进填写（`fill-engine/stepnav.ts`）、P1 自动采集多页 HTML（`fill-engine/collect.ts`）、P2 进度可视化（`renderer/src/stepview.ts`），并配套 `ParsedQuestion.step`、`FillConfig.stepWaitTimeout/maxSteps/prefillOtherSteps`、`FillReport.steps/stepError/prefilled`、`FillProgress.step/totalSteps/stepIndex/stepTotal` 等字段与 5 个样例、7 个测试文件。

**2026-09-19 应用户要求整体移除**（维护成本高于收益）。上述模块、字段、样例、测试与 UI 入口（分页块编辑器、采集按钮、表格按页分组、预填开关）全部删除，`engine.ts` 主循环恢复为单次线性遍历。原始实现完整保留在 `.workbuddy/backup/paged-rollback-20260919/`，需要时可恢复。

（注：断点续填本身不属于分页功能，已保留；它此前在 M6 界面重构时丢了入口，现仍可从左栏「只填未完成项（续填）」启用。）

### 多页问卷改为「接着填」（复用浏览器会话，2026-09-19）

上条「自动翻页」方案移除后，多页需求改用更轻的**用户主导 + 会话复用**方案落地：主进程按任务记住浏览器窗口（`fill-engine/session.ts`），`runFill` 支持 `getSession`/`onSessionReady` —— 有存活会话则**直接复用、不重开、不重新导航**（`FillReport.reusedSession` 标记），于是「在浏览器里手动翻到下一页 → 抓取本页 HTML（`fill-engine/grab.ts`）→ 重新生成题目 → 再运行」能在原窗口当前页接着填，而不回到第 1 页。配套左栏「浏览器会话」区（状态 / 抓取当前页 HTML / 结束会话）；会话失效（窗口被关）或目标链接变更时自动回退为重新打开。该方案**不引入**页码字段、不自动识别/点击翻页按钮，把翻页交给用户，因此没有原方案的维护成本。

2026-09-19 追加（当日再调整一轮）：抓取不再依赖「先跑一次运行」—— 原先有死锁（抓取要求已有浏览器会话，而「开始填写」是唯一的开会话入口、却又要求先有题目）。现在把两件事**彻底拆开**：`fill:session:open` 只负责**打开**（`session.ensureSession` + `browser.openPageAt`；题目/答案还没生成也能先把页面开起来），`fill:session:grab` 只负责**抓取、且必须已有窗口**（不再自行开浏览器）。抓取前先经 `grab.waitForGrabReady` 等页面真正长好（`readyState` 不再 loading 且页面已出现表单控件或正文，最多 15 秒）—— 早先「打开即抓」会抓到前端还没渲染的空壳，用户明确否掉了这个行为。抓取覆盖前自动建 `pre-grab` 快照（**连 HTML 一起**，回滚可连页面还原，第一次的记录不会丢）；抓取成功后由 AI 按页面内容自动命名任务（`TaskConfig.nameAuto`：只覆盖自动生成的名字，用户改过的不动）。


