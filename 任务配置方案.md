# 任务配置方案（M7）

> 版本：v1.1
> 日期：2026-09-19
> 状态：**已实施**（M7.1–M7.5 全部完成，版本 0.3.0；实施中与本文的差异见 §17）
> 关联文档：[客户端方案.md](客户端方案.md)（总体设计；本文替代其 §9「数据与状态」）、[AI内置方案.md](AI内置方案.md)、[README.md](README.md)
> 替代范围：原 `userData/projects/` 的「项目名 + 保存 / 加载 / 删除」机制（已移除）

---

## 1. 背景与问题

现行机制由 `src/main/index.ts` 的 4 个 `projects:*` IPC + `App.tsx` 的「项目栏」构成，与「多任务并发」的定位存在 6 处结构性冲突：

| # | 现状 | 问题 |
|---|------|------|
| 1 | 任务标签（tab，内存态）与项目（disk 快照）**两套容器并存** | 职责交叉：用户要先给「项目名」再点保存，还要从下拉里挑项目加载；tab 名（任务 1/2/3）与项目名对不上，分不清哪个标签对应哪份已存配置 |
| 2 | 「保存」实际是**另存为**：`writeFileSync` 写 `projects/<名>/project.json` | 改名后点保存会多出一份而不是重命名；命名即身份，无法保持任务身份 |
| 3 | 同名保存**静默覆盖** | 无确认、无历史版本 —— 所谓「备份」能力实际不存在 |
| 4 | 删除 = `rmSync(dir, { recursive: true, force: true })` | **永久、不可逆、无二次确认**，而按钮紧挨「加载」 |
| 5 | **无自动持久化** | 关标签 / 关应用即丢现场；多任务并发时要求用户手工维护快照，不现实 |
| 6 | index 缺失：下拉里只有名字 | 无保存时间、无题目数，选不出该用哪个；也无导出/导入通道，换机不可迁移 |

**结论：** 问题不在某个按钮，而在「任务」与「项目」这两个概念的冗余。新方案统一为**单容器**：任务即文档，改动自动落盘，删除可逆，历史可回溯。

---

## 2. 目标与非目标

### 目标（已确认方向）

1. **自动持久化** —— 任务标签本身就是持久化实体，改动自动落盘，重启自动恢复上次的全部任务；**取消「项目名 + 保存 / 加载」整行 UI**。
2. **删除可逆** —— 删除只做软删除（移入回收站），顶部 toast 一键撤销；回收站保留 30 天后清理，亦支持手动彻底删除。
3. **备份到位** —— 自动时间戳快照（可回滚任意历史点）+ 单文件导出 / 导入（换机、交接）。

### 非目标

- 不做云端同步 / 多设备同步（仍是本机 JSON 存盘）。
- 不做协作、权限、分享链接。
- 不做增量 diff 存储（单任务配置仅数 KB，全量写 + hash 去重已足够）。
- 不改动填写引擎与 AI 模块的任何行为（本方案只动存储与任务管理 UI）。

---

## 3. 核心决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 容器模型 | **任务 = 标签 = 落盘实体**（单一概念），废弃「项目」 | 消除两套容器的心智负担；「多任务并发」天然要求每个标签独立持久化 |
| 任务身份 | **稳定 `id`（目录名）+ 可变 `name`（显示名）** | 重命名不再是删除+新建；重名可共存（id 唯一），导出/快照/回收站都按 id 定位 |
| 持久化时机 | 渲染侧**立即**发 patch，主进程做 500ms 防抖 + 串行写盘 | 把 pending 放在 main，`before-quit` 能保证 flush；renderer 侧防抖会在退出瞬间丢改动 |
| 写盘方式 | **临时文件 + rename 原子替换** | 避免写一半崩溃产生半截 JSON；Windows 上 Node `fs.rename` 走 `MoveFileEx(REPLACE_EXISTING)`，可覆盖 |
| 删除方式 | **同卷 rename 移入 `trash/`**（软删除） | rename 是原子操作，不复制数据、不丢内容；回收站 = 普通目录，可手动翻查 |
| 快照触发 | 关键动作立即快照 + 5 分钟时间桶 + **内容 hash 去重** + 每任务保留 20 份 | 自动持久化下「每次改动都快照」会爆炸；hash 去重避免一堆完全相同的版本 |
| HTML 源码 | 存 `tasks/<id>/source.html`，**不参与快照、默认不随导出** | HTML 是体积大头（几十~几百 KB）；粘一次很贵所以要存，但快照/导出只关心轻量配置 |
| 迁移 | 首次启动自动迁移 `projects/` → `tasks/`，**原目录改名保留**不删除 | 迁移失败可回退；符合「备份」精神 |

---

## 4. 存储布局

```
userData/
├── tasks/
│   ├── index.json                      # 任务列表索引（顺序 + 元数据，不含题目/答案）
│   ├── t-7fk3a91c/
│   │   ├── config.json                 # 当前配置（原子写）
│   │   ├── pages/                      # 多页问卷的页面存档（一页一份 HTML）
│   │   │   ├── p1.html                 # 第 1 页（旧布局的 source.html 会在首次写入时迁到这里）
│   │   │   └── p2.html
│   │   ├── source.html                 # 旧布局遗留：单页 HTML（只读透，写入时自动迁移到 pages/p1.html）
│   │   └── snapshots/
│   │       ├── 20260919-131500-a1b2.json
│   │       ├── 20260919-131500-a1b2.html   # 快照若含 HTML，正文放这个旁挂文件
│   │       └── …（最多 20 份，滚动淘汰最旧）
│   └── t-2mp8q04d/ …
├── trash/
│   └── t-7fk3a91c/                     # 软删除：整目录 rename 进来
│       ├── config.json
│       ├── snapshots/ …
│       └── meta.json                   # { "deletedAt": "...", "name": "..." }
├── migration.json                      # { "from": "projects", "at": "...", "count": 3, "backupDir": "projects.migrated-…" }
└── output/                             # 报告 / 截图 / error.log（维持现状）
```

**为什么 `index.json` 独立存在**：任务面板要显示「名称 / 题目数 / 更新时间」，若每次都扫 `tasks/*/config.json` 全量读，任务一多就慢；索引让列表页只读一个小文件。索引进度可重建（见 §13）。

---

## 5. 数据模型

```ts
/**
 * 单个任务的完整配置（= 现行 ProjectData 的替代，落 config.json）
 * 注：登录态不落配置、也没有开关 —— 每个任务固定用 `<userData>/profile/<id>` 作浏览器
 *     profile 目录，登录一次长期有效（cookie 存在那个目录里）。
 */
interface TaskConfig {
  id: string                       // 稳定身份，目录名，创建后不变
  name: string                     // 显示名，可随时重命名，允许重名
  link: string
  channel: string                  // '' | 'chrome' | 'msedge'
  pages: TaskPage[]                // 页面存档，**至少一页**（老数据载入时迁移成单页）
  activePageId: string             // 当前正在编辑 / 填写的那一页
  questions: Question[]            // ↓ 下面是 activePage 的**镜像**，不是独立数据
  answers: Record<string, string>
}

/**
 * 多页问卷的一页存档：HTML 存 `pages/<pageId>.html`，题目与答案跟着这一页走。
 * 「多页不必反复花 AI 额度」靠它实现 —— 翻回旧页直接沿用该页的题目。
 */
interface TaskPage {
  id: string                       // = HTML 文件名，创建后不变
  name: string                     // 页名（页面标题 / 「第 N 页」/ 用户改名）
  nameAuto?: boolean               // 名字是否程序自动起的
  capturedAt?: string
  url?: string
  fp?: string                      // 该页 HTML 的精确指纹（renderer freshness.fingerprint）
  sig?: string                     // 该页 HTML 的结构签名（忽略属性与长数字，用于判「同一页」）
  questions: Question[]
  answers: Record<string, string>
  questionsFor?: string
  answersFor?: string
}

/** index.json 的一项：列表页/标签栏只需它 */
interface TaskIndexItem {
  id: string
  name: string
  updatedAt: string                // ISO
  createdAt: string
  questionCount: number
  answerCount: number
  hasHtml: boolean                 // 是否保留了 source.html
  order: number                    // 标签栏顺序（拖拽排序用）
}

interface TaskIndex {
  version: 1
  tasks: TaskIndexItem[]
}

/** 快照元信息（快照文件本身就是一个 TaskConfig 的瘦身拷贝） */
interface SnapshotMeta {
  ts: string                       // 主键（文件名主干），同一秒内的多次快照会追加序号
  createdAt: string
  questionCount: number
  answerCount: number
  size: number                     // 字节
  label: 'auto' | 'manual' | 'pre-rollback' | 'key-action'
  hash: string                     // 内容 hash，自动快照去重用
}

interface TrashItem {
  id: string
  name: string
  deletedAt: string
  questionCount: number
  expiresAt: string                // deletedAt + 保留期，用于 UI 显示剩余天数
}
```

**HTML 不落进 `TaskConfig`**：每页的 HTML 单独存 `pages/<pageId>.html`（旧布局的 `source.html` 作为第 1 页被读透，写入时自动迁到 `pages/p1.html` 并删掉旧文件），因此 `config.json` 永远只有几 KB，快照与导出都保持轻量。加载/复制任务时按需读取。

**顶层 `questions`/`answers`/指纹是当前页的镜像**：这么设计是为了让填写引擎、快照、导出、`state/<id>.json` 这些「只认一份内容」的既有代码不必知道「页」的存在 —— 写 patch 时自动落到 `activePageId` 那一页，读回来时由该页填出。`getHtml(id)`/`setHtml(id, html)` 同样退化为「当前页」的别名。界面（页面条）则直接读写 `pages`。

---

## 6. 自动持久化

**时序（renderer → main）：**

```
用户改动（输入链接 / 导入题目 / 改答案 / 改开关）
   │  立即
   ▼
window.api.tasksPatch(id, patch)          ← 无防抖，保证不漏
   │  500ms 防抖（按 taskId 独立计时）
   ▼
main: 写入队列（同一 taskId 串行）→ 临时文件 → rename 原子替换
   │
   ▼
更新 index.json 的 updatedAt / questionCount / answerCount
   │
   ▼
回推 tasks:state 事件 → 顶栏显示「已自动保存 · 刚刚」
```

**关键点：**

1. **防抖在 main 侧**，renderer 立即发送。这样应用退出时 pending 都在 main，`app.on('before-quit')` 里同步 flush 即可，不会丢最后 500ms 的输入。
2. **同任务串行写、且全同步**：`fs` 全部用同步 API，单线程下天然串行，不存在两次写乱序；也因此 `before-quit` 的 flush 一定能写完（异步写在退出瞬间不可靠）。
3. **原子写**：写 `config.json.tmp` → `fs.renameSync(tmp, dest)`。崩溃最坏情况是留下一个 `.tmp`，下次启动清理（§13）。
4. **不写 HTML 到 config**：`source.html` 只在粘贴 HTML 时单独落盘一次。
5. **写盘失败不静默**：捕获后向 renderer 推 `tasks:error`，顶栏变红「保存失败，点击重试」，同时按现有机制追加 `userData/output/error.log`。
6. **顶栏保存态**：`保存中… / 已自动保存 · 刚刚 / 保存失败 ⚠`，让用户明确知道「不用再点保存了」。

---

## 7. 快照与回滚

**触发条件（满足任一）：**

| 类型 | label | 说明 |
|------|-------|------|
| 关键动作 | `key-action` | 导入题目 / 导入答案 / 回滚 / 导入文件 —— 立即打一份 |
| 时间桶 | `auto` | 首次改动起每 5 分钟最多 1 份；内容 hash 与上一份相同则跳过 |
| 手动 | `manual` | 任务面板里的「新建快照」 |
| 回滚前 | `pre-rollback` | 回滚前先存当前状态 —— 回滚本身也可撤销 |

**保留策略：** 每任务最多 20 份，超出按 `createdAt` 淘汰最旧的 `auto`（`manual` / `pre-rollback` 优先保留）；`manual` 不受 20 份上限约束。

**回滚流程：** 面板「历史版本」列表（时间 / 题目数 / 答案数 / 来源标签）→ 选一份 → 预览摘要 → 确认回滚 → 先打 `pre-rollback` 快照 → 用快照内容覆盖 `config.json` → 通知 UI 重载。

**去重：** 快照内容做 SHA-256，**仅对 `auto` 生效** —— 与最新一份 hash 相同则跳过，避免自动保存在高频改动下堆出大量重复版本；`manual` / `key-action` / `pre-rollback` 是用户或流程的明确意图，一律留档（同一秒内多次快照会追加序号，不会互相覆盖）。

**首次改动的行为：** 一个任务在本会话中的第一次改动会立即留一份 `auto`（此时没有可比对的上一份），保证「会话起始状态」可回溯；之后按时间桶节流。

---

## 8. 回收站与撤销

> 命名：**机制/目录仍叫 `trash/`（回收站）**，界面上面向用户的那一层叫「**历史任务**」（2026-09-19 起）。理由：用户看到的是「我删过的任务还在这儿」，不是「垃圾桶」。

| 动作 | 实现 |
|------|------|
| 删除任务 | `rename(tasks/<id>, trash/<id>)` + 写 `meta.json` + 从 `index.json` 移除。**不弹确认框**（因为可撤销） |
| 撤销 | 顶部 toast：「已删除「任务名」· 撤销」，停留 8 秒。点撤销 = `rename` 回 `tasks/` + 恢复 index 项 + 重新激活标签 |
| 恢复 | 回收站列表 → 「恢复」→ 同上；若 id 已被新任务占用（极端情况）则改名为 `name (1)` |
| 彻底删除 | **二次确认**，文案明确「此操作不可恢复」；确认后 `rmSync(trash/<id>, { recursive: true })` |
| 自动清理 | 应用启动时 + 每 24h，清理 `deletedAt` 超过保留期的条目（默认 30 天，设置可选 7 / 30 / 90 / 永久） |
| 清空回收站 | 二次确认后清空 |

**设计取向：** 因为删除不再可怕，所以删除**不需要**确认弹窗（确认弹窗反而打断操作流）；把「确认」让给真正不可逆的「彻底删除」。这是与现行机制最关键的行为差异。

**注意：** 回收站与快照都是本地明文 JSON，若配置包含个人答案信息，用户应知晓导出文件同样含答案（导出时给一行提示）。

---

## 9. 导出与导入

**导出（`dialog.showSaveDialog`）：**

```json
{
  "format": "form-filler-task",
  "formatVersion": 2,
  "exportedAt": "2026-09-19T13:20:00+08:00",
  "tasks": [
    {
      "name": "XX 满意度问卷",
      "link": "https://example.com/form",
      "channel": "",
      "questions": [],
      "answers": {},
      "sourceHtml": "<form>…</form>"
    }
  ]
}
```

- 入口：任务面板「导出当前任务」/「导出全部任务」。
- 勾选项「包含 HTML 源码」（默认**不勾**，因为体积大）。
- 文件名默认 `<任务名>-20260919.json`，非法字符替换。

**导入（`dialog.showOpenDialog`）：**

1. 校验 `format` 与 `formatVersion`（不认识则报错并说明期望版本）。
2. 结构校验复用现有 `parseImportedQuestions` 的思路，逐任务规范化；单条任务失败不阻断其余。
3. **一律作为新任务插入**，绝不覆盖现有任务；id 重新生成；`name` 冲突自动加后缀 `(2)`；`sourceHtml` 落成 `source.html`。
4. 结果提示：「已导入 2 个任务；1 个失败（原因：…）」。

---

## 10. IPC 接口

**废弃**（renderer 不再调用，保留一版供迁移兜底后删除）：`projects:list` / `projects:save` / `projects:load` / `projects:delete`

**新增：**

```ts
tasks:list           ()                       => TaskIndexItem[]      // 只读 index，轻量
tasks:get            (id)                     => TaskConfig           // 单任务全量（含题目/答案）
tasks:create         (name?)                  => TaskIndexItem
tasks:patch          (id, patch)              => TaskIndexItem        // 高频；main 内防抖+串行+原子写
tasks:rename         (id, name)               => TaskIndexItem
tasks:duplicate      (id)                     => TaskIndexItem        // 复制（含题目/答案，可选带 HTML）
tasks:reorder        (idsInOrder)             => void
tasks:snapshots      (id)                     => SnapshotMeta[]
tasks:snapshotCreate (id)                     => SnapshotMeta
tasks:snapshotRead   (id, ts)                 => TaskConfig           // 供预览
tasks:rollback       (id, ts)                 => TaskConfig
tasks:getHtml        (id)                     => string | null        // 「当前页」HTML 的别名（历史接口）
tasks:setHtml        (id, html)               => boolean              // 同上，写到当前页
// ---- 多页问卷的页面存档 ----
tasks:pageCreate     (id, {name?,html?,url?,fp?,sig?}) => TaskConfig  // 建页并切成当前页
tasks:pageSelect     (id, pageId)             => TaskConfig           // 切页（顶层镜像随之切换）
tasks:pagePatch      (id, pageId, patch)      => TaskConfig           // 改页名 / 补指纹 / 改该页题目答案
tasks:pageHtml       (id, pageId)             => string | null
tasks:pageSetHtml    (id, pageId, html)       => boolean
tasks:pageDelete     (id, pageId)             => TaskConfig           // 最后一页时等价于清空该页
tasks:export         (ids | 'all', includeHtml) => { canceled: boolean; path?: string }
tasks:import         ()                       => { canceled: boolean; imported: TaskIndexItem[]; failed: {name, reason}[] }
tasks:trash          (id)                     => void                 // 软删除
trash:list           ()                       => TrashItem[]
trash:restore        (id)                     => TaskIndexItem
trash:purge          (id)                     => void                 // 彻底删除（renderer 先二次确认）
trash:empty          ()                       => void

// ---- 浏览器会话（多页问卷：复用窗口 + 抓取 + 翻页） ----
fill:session:open      (taskId, {link?, channel?}) => { ok, opened?, url?, error? }   // 只打开，不抓取
fill:session:grab      (taskId, {link?, waitMs?})  => { ok, html?, url?, frames?, archived?, next?, error? }
fill:session:next      (taskId)                    => { ok, next: NextButton[], error? }  // 只读识别
fill:session:click-next(taskId, {selector?,text?,frameUrl?}) => { ok, strategy?, url?, ready?, readyHint?, error? }
fill:session:status    (taskId)                    => { open, url? }
fill:session:close     (taskId)                    => boolean
fill:session:reset-profile (taskId)                => { ok, error? }      // 清除登录态（关窗口 + 删 profile 目录）
```

**事件（main → renderer）：** `tasks:state`（保存态：saving / saved / error）、`tasks:changed`（index 变更，供多窗口/面板刷新）。

**preload** 同步在白名单里补 `tasks*` / `trash*`，与现有 `fillRun` / `projects*` 写法保持一致。

---

## 11. UI 改造

### 11.1 删除的东西

顶部的「项目栏」整行消失：~~项目名输入 / 保存 / 已存项目下拉 / 加载 / 删除~~。

### 11.2 新的形态

```
┌ 表单填写器 ─────────────────────── [已自动保存][任务管理]            ┐
│                                                                      │
│  任务标签： [满意度问卷 ▾×] [任务 2 ×] [＋]   ← 双击重命名、拖拽排序  │
│                                                                      │
│  ┌ 侧栏（260px） ──┐  ┌ 步骤流水线（沿用现有 TaskFlow） ──────────┐  │
│  │ 目标设置        │  │ ① 表单 HTML（启动浏览器 / 抓取在此，      │  │
│  │  链接/浏览器/   │  │   侧栏不重复放一份）                     │  │
│  │  提交/续填      │  │   ├ 页面存档：① ② ③（切页/改名/删除）   │  │
│  │ 浏览器会话      │  │   └ 识别到的翻页按钮：[下一页][下一步]   │  │
│  │  状态/结束会话/ │  │ ② 生成题目（只针对当前页）              │  │
│  │  清除登录态     │  │ ③ 生成答案                             │  │
│  │ 任务操作        │  │ ④ 校对题目与答案（含运行结果）          │  │
│  │  复制/导出      │  └────────────────────────────────────────┘  │
│  │  导入/清空内容  │                                              │
│  └────────────────┘                                              │
│                                                                      │
│  [开始填写] [停止]  状态文案 …        绝不自动提交 · 填完人工核对      │
└──────────────────────────────────────────────────────────────────────┘

「任务管理」右侧抽屉：
  ┌ 任务（3） │ 历史任务（2） ───────────────────────────┐
  │ ● 满意度问卷                        [重命名][复制]   │
  │   12 题 · 已答 12 · 更新于 2 分钟前  [历史版本][导出] │
  │   ┌ 历史版本 ─────────────────────────┐  [清空内容]  │
  │   │ 2026/9/19 13:20  自动  12 题 · 4KB │  [删除]      │
  │   │ 2026/9/19 13:05  关键动作  12 题   │  [回滚到此版本]│
  │   └───────────────────────────────────┘              │
  │ [导入配置] [导出全部任务] [新建任务]                  │
  └──────────────────────────────────────────────────────┘
```

### 11.3 要素说明

| 要素 | 行为 |
|------|------|
| 标签栏 | 双击标签名就地重命名（立即落盘）；拖拽调整顺序；`×` = 软删除 + 撤销 toast |
| 「＋」 | 新建任务，默认名「任务 N」，自动落盘并激活 |
| 保存态指示灯 | 顶栏右侧胶囊：保存中… / 已自动保存 / 保存失败，点击重试（仅在失败时） |
| 「任务管理」 | 右侧抽屉，含「任务」「历史任务」两个分页；任务项显示题目数 / 已答数 / 相对更新时间 |
| 「历史版本」 | 展开快照列表（时间、来源标签、题目数、体积）+「新建快照」+「回滚到此版本」 |
| 侧栏「任务操作」 | 复制任务、导出、导入配置、清空内容（**不再放任务名输入框**：重命名统一走「双击标签栏」，避免两处入口） |
| 「清空内容」 | 语义明确为清空当前任务的内容（链接 / HTML / 题目 / 答案），**保留任务本身与名字**，二次确认；清空前自动留 `pre-rollback` 快照 |
| 关闭标签 | 即软删除（含撤销 toast）；若任务为空（无题目、无答案、无 HTML）则直接彻底删除、不进历史任务，避免垃圾堆积 |
| 历史任务（原「回收站」） | 界面叫「历史任务」（磁盘目录仍是 `trash/`）：放的是**删掉的任务**，与单任务的「历史版本」不是一回事；「恢复」可回到标签栏，「彻底删除」「清空历史任务」就地二次确认（就地展开确认，不弹模态）；保留期可选 7 / 30 / 90 天或永久 |
| 空列表 | 任务被全部删除时主区显示「暂无任务」占位，不再强行造一个空任务 |

### 11.4 与旧交互的关键差异

| 旧 | 新 |
|----|----|
| 要输「项目名」再点「保存」 | 没有保存按钮：改什么存什么 |
| 下拉选项目 → 点「加载」 | 标签一点即切，内容按需加载 |
| 「删除项目」永久删除 | 「×」软删除 + 撤销 toast |
| 「清空当前任务」直接清空 | 「清空内容」二次确认 + 自动留历史版本 |
| 换机只能手动抄配置 | 导出 / 导入单文件，可整批搬

---

## 12. 迁移（M7 首次启动，一次性）

1. 检测 `userData/projects/` 存在且 `migration.json` 不存在：
2. 遍历 `projects/<目录名>/project.json` → 为每个生成 `tasks/<新 id>/config.json`（`name` = 原目录名，`createdAt/updatedAt` 取文件 mtime）。
3. 重建 `index.json`。
4. `rename(projects/ → projects.migrated-<yyyyMMdd-HHmmss>/)`——**保留原目录，不删除**，用户确认无误后可自行删除。
5. 写 `migration.json` 记录迁移数量与备份目录名。
6. UI 首次启动弹一条可关闭的提示：「已迁移 3 个旧配置（原目录已备份为 `projects.migrated-…`）」。
7. 单个 `project.json` 解析失败 → 跳过并记录，不阻断整体迁移。

---

## 13. 边界与异常

| 场景 | 处理 |
|------|------|
| 写盘失败（磁盘满 / 权限） | 顶栏红字「保存失败」+ 重试按钮 + 追加 `output/error.log`；**不假装成功**，不静默丢弃 |
| 崩溃留下 `config.json.tmp` | 启动扫描：有 tmp 无 dest → tmp 提升为 dest；两者都在 → 删 tmp（tmp 是不完整写入，dest 更可信） |
| `config.json` 损坏 | 改名为 `config.corrupt-<ts>.json` 保留现场，用最近一份快照自动恢复（恢复失败则视为空任务） |
| `index.json` 缺失/损坏 | 扫各任务目录的 `config.json` 重建（name 取 config.name，createdAt/updatedAt 取文件 mtime），按 createdAt 升序重排 |
| 任务名为空 | 新建未给名 → 自动「任务 N」；改名输入框删空 → **保留原名**（不落空名）；允许重名（id 唯一） |
| 极端情况 id 冲突（恢复回收站时） | 重新生成 id，并对名字做去重后缀（`问卷` → `问卷 (2)`）后恢复 |
| 快照目录被用户手动删空 | 视为「无历史版本」，不报错 |
| 应用被强杀 | 最坏丢最后 500ms 内的改动；重启后从 `config.json` 恢复上次落盘状态 |
| 任务很多（50+） | 列表页只读 `index.json`，`config` 懒加载；标签栏只渲染当前激活任务的完整内容 |

---

## 14. 里程碑

| 里程碑 | 内容 | 验收 |
|--------|------|------|
| **M7.1 存储层** | `tasks/` 布局 + `TaskConfig`/`TaskIndex` 类型 + 原子写 + 防抖串行队列 + index 维护 + 快照读写 + 回收站目录操作 + 迁移逻辑；纯函数与 fs 操作单测 | 单测覆盖：原子写、tmp 残留清理、config 损坏恢复、index 重建、快照去重与淘汰、trash rename/restore、迁移（含损坏文件跳过） |
| **M7.2 IPC + preload** | 新增 `tasks:*` / `trash:*`，废弃 `projects:*`；preload 白名单与类型声明同步 | typecheck 全绿；渲染侧可创建/改/删/恢复 |
| **M7.3 自动持久化接线** | renderer 立即发 patch + 顶栏保存态 + `before-quit` flush | 改一处答案 → 关进程 → 重开，内容仍在；关在 500ms 内也不丢 |
| **M7.4 任务管理 UI** | 去掉项目栏；标签重命名/排序；任务管理抽屉（任务 + 回收站）；撤销 toast；历史版本与回滚；清空语义调整 | 手工走查：重命名不失身份、删除可撤销、回滚前后可逆 |
| **M7.5 导入导出 + 收尾** | 导出/导入单文件 + 二次确认文案统一 + README / 客户端方案.md §9 更新 + 版本 0.3.0 | 在另一台机器导入导出文件，题数与答案数一致 |

---

## 15. 风险

| 风险 | 对策 |
|------|------|
| 自动写盘频率高导致 IO 压力 | 主进程 500ms 防抖 + 同任务串行 + 仅写数 KB 的 `config.json`；HTML 只在粘贴时写一次 |
| 用户以为「没有保存按钮 = 没保存」 | 顶栏常驻保存态（已自动保存 · 刚刚）+ 首次启用时一条一次性提示 |
| 回收站与快照堆积占空间 | 快照每任务上限 20 份 + hash 去重；回收站 30 天自动清理；两者都是文本 JSON，量级可控 |
| 迁移误伤旧数据 | 原目录只改名不删除 + `migration.json` 留痕 + 单文件失败跳过 |
| 数据丢失（写坏 / 崩溃） | 原子写 + tmp 恢复 + 损坏自动回滚到最近快照 + 三份兜底（config / snapshots / trash） |
| 导出文件泄露答案 | UI 明确提示「导出文件包含答案内容」；不做自动上传，全部本地 |

---

## 16. 验收标准

- [ ] 关闭应用再打开，上次的全部任务标签、名称、链接、题目、答案原样恢复
- [ ] 编辑后 1 秒内落盘；在防抖窗口内直接杀掉进程，改动仍保留
- [ ] 界面上不存在「保存 / 加载 / 项目名」入口
- [ ] 重命名任务不产生新副本，身份（id）不变
- [ ] 删除任务后 toast 可撤销，撤销后内容与删除前逐字段一致
- [ ] 回收站条目显示剩余保留天数；超过保留期自动清理；「彻底删除」有二次确认
- [ ] 快照：关键动作后必有快照；相同内容不重复落盘；单任务最多 20 份；回滚前自动留 `pre-rollback`
- [ ] 导出 → 换机器导入：任务数、题目数、答案数完全一致
- [ ] 手工制造写盘失败（只读目录），界面出现红字「保存失败」且 `error.log` 有记录，不静默
- [ ] 手工损坏 `config.json`，启动后自动从快照恢复并提示
- [ ] 旧 `projects/` 自动迁移完成，原目录以 `projects.migrated-*` 保留
- [ ] 填写引擎 / AI 模块回归正常（本方案不改变其行为）

---

## 17. 实施记录（v0.3.0）

### 17.1 落地位置

| 层 | 文件 | 说明 |
|----|------|------|
| 存储 | `src/main/tasks/store.ts` | `createTaskStore(root, options)` 工厂，全部同步 fs；可选注入 `now()` / `debounceMs` 以便测试 |
| | `src/main/tasks/fsx.ts` | 原子写、可区分「缺失/损坏」的读取、`.tmp` 崩溃恢复、目录 move |
| | `src/main/tasks/sanitize.ts` | 导入数据清洗（题目 / 答案 / selector 归一化） |
| 类型 | `src/main/tasks/types.ts` | `TaskConfig` / `TaskIndexItem` / `SnapshotMeta` / `TrashItem` / `ExportFile` / `TaskStoreSettings` |
| IPC | `src/main/tasks/ipc.ts` | `registerTaskHandlers({ store, getWindow, broadcast })`，含导出/导入的系统对话框 |
| 接线 | `src/main/index.ts` | `whenReady` 建 store → 注册 IPC → 迁移 → 清理过期回收站 → 建窗；`before-quit` flush |
| 桥 | `src/preload/index.ts` | `tasks*` / `trash*` / `onTasksState` / `onTasksChanged` / `startupNotice` |
| UI | `src/renderer/src/App.tsx` | 任务列表为唯一数据源；自动持久化接线；撤销 toast；确认/信息弹窗 |
| | `src/renderer/src/components/TaskManager.tsx` | 任务管理抽屉（任务 / 回收站两个分页、历史版本、就地二次确认） |
| 测试 | `src/main/tasks/__tests__/store.test.ts` | 27 个用例（临时目录 + 可控时钟） |

### 17.2 与本文的差异（实现修正）

1. **不建异步写队列**：改为一律同步原子写（详见 §6 第 2 点）——同步天然串行，且让退出 flush 可靠。
2. **快照去重只作用于 `auto`**：`manual` / `key-action` / `pre-rollback` 一律留档（详见 §7）；`SnapshotMeta` 因此多了 `hash` 字段，`ts` 在同一秒内冲突时追加序号。
3. **首次改动即留一份 `auto` 快照**：避免「会话开始时是什么样」无从回溯。
4. **快照排序以 `createdAt`(ms) 为主序**、`ts` 仅兜底（同一秒内的多份快照文件名带随机 hash，不能靠文件名排序）。
5. **`patch` 会拦截空名字**：界面把名字输入框删空时保留原名，不留空名落盘。
6. **UI 落在现有「侧栏 + 流水线」布局上**：原计划的顶栏方案改为——顶栏放保存态 + 任务管理，侧栏原「项目配置」区块换成「当前任务」（详见 §11）。
7. **空列表不再强行造任务**：删除到零个任务时主区显示占位与新建入口；仅**首次运行**（从未有过任务）会自动建一个。
8. **「彻底删除」的就地二次确认**：不弹模态，在行内展开确认，减少打断。

### 17.3 验证结果

- `vitest run`：存储层 27 + AI 12 + AI 18 + 相似度 7 = **64 通过**（另有 2 个 fill-engine e2e 用例因本沙箱 Playwright `browser.close()` 挂起而超时，与本改动无关）。
- `tsc --noEmit`（node / web）：通过。
- `electron-vite build`：通过（main 60.5 kB / preload 4.3 kB / renderer 290 kB）。
- IPC 通道逐条核对：preload ↔ main 无缺漏，renderer 无残留 `projects*` 调用。

### 17.4 尚未做到的

- 验收清单中「手工制造写盘失败」「换机器导入导出」两项需在真实环境手工走查（单测已覆盖导入导出往返与损坏恢复）。
- 快照当前无「预览差异」能力，只能看元信息（时间 / 题数 / 体积）后整体回滚。

---

## 18. 增补：多页问卷的页面存档（v0.5）

多页问卷原本只有「一份 HTML + 一份题目」，翻到下一页再抓就把上一页覆盖掉，AI 额度白花、也没法回头填。
v0.5 引入**页面存档**：每页各自留着自己的 HTML / 题目 / 答案。

| 关注点 | 落法 |
|---|---|
| 存储 | `tasks/<id>/pages/<pageId>.html`；旧 `source.html` 当第 1 页被读透，写入时迁到 `pages/p1.html` 并删旧文件 |
| 兼容 | 顶层 `questions`/`answers`/指纹 = 当前页的镜像；`getHtml`/`setHtml` 退化为当前页别名；老数据（无 `pages`）载入时迁移成单页 —— 引擎、快照、导出、断点续填都不用改 |
| 建页 | `create()` 就落一个空的「第 1 页」（任务永远至少一页，界面才有落点）；`pageCreate` 建页后切成当前页 |
| 抓取落页 | **判定在渲染层**（`src/renderer/src/pages.ts` 的 `routeGrab`）：精确指纹 `fp` 命中 → 只更新该页 HTML；结构签名 `sig` 命中 → 认作同一页（更新 HTML 与 `fp`，并把题目指纹重盖到新 `fp`，题目继续有效）；空占位页 → 填进去；都没命中 → `pageCreate` 存新页 |
| 结构签名 | 剥 `script`/`style`/注释、**丢掉全部标签属性**、压掉 4 位以上数字后取指纹 —— 腾讯问卷那类「每次渲染换一截随机前缀」的页面靠它认出「还是同一页」 |
| 页名 | 抓取时取 `<title>` → 第一个 `<h1-3>`，都没有则「第 N 页」；`nameAuto` 标记保证用户改过就不再动 |
| 删页 | 落回邻居（优先后一页）；只剩一页时等价于「清空该页」（不破坏「至少一页」不变式） |
| 快照/导出 | 快照连**每页 HTML** 一起存（旁挂 `.html`），回滚整页还原；导出带 `pages[]`（含 HTML 正文），导入按页重建；老导出文件按顶层字段合成单页 |
| 重复题 | 新页生成题目时，把**上一页里题干与选项完全相同**的题的答案带入（`carryAnswers`），写完把 `answersFor` 置为这版题目的指纹 |
| 翻页按钮 | 引擎 `next-button.ts`：**只读**扫描（关键词打分，命中「提交/完成/返回/上一步/取消」一律排除，隐藏/0 面积排除，禁用照实标注），主 frame 优先、再子 frame；点击三级回退（标记选择器 → `role=button` 文本 → 文本派发）。**只有用户点界面 chip 才翻页** |

**未做**：`same-page` 自动重盖指纹是「结构一致即沿用」的乐观判断（属性随手改而结构不变时选择器可能已失效）——
留了两条退路：第 2 步永远可以重新导入题目；生成的题目里若选择器失效，填写时会在第 4 步表现为 failed 并附截图。
